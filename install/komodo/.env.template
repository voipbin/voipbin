# Komodo - web UI container management (VOIP-1339)
# Generated by: scripts/komodo.sh init (auto-generated, existing values
# preserved - idempotent). Do not hand-edit the secret values unless you
# know what you're doing; see docker-compose.yml.dist's comments on why
# regenerating them requires also wiping the komodo_mongo_data volume.

# Loopback bind for the web UI (SSH local port-forward is the access path,
# see README.md "Access") - same posture as every other admin port in the
# main stack (db/redis/rabbitmq/prometheus/grafana).
KOMODO_BIND_ADDRESS=127.0.0.1
KOMODO_HOST=http://localhost:9120
KOMODO_TITLE=Komodo
KOMODO_SERVER_NAME=bm-nyc-01

KOMODO_ADMIN_USERNAME=admin
KOMODO_ADMIN_PASSWORD=your-random-komodo-admin-password
KOMODO_DATABASE_USERNAME=komodo
KOMODO_DATABASE_PASSWORD=your-random-komodo-db-password
KOMODO_JWT_SECRET=your-random-jwt-secret
KOMODO_WEBHOOK_SECRET=your-random-webhook-secret
